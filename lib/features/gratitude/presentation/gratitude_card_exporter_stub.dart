import 'dart:typed_data';

Future<String> saveGratitudeCardImage({
  required Uint8List bytes,
  required String filename,
}) {
  throw UnsupportedError('Saving gratitude card images is not supported here.');
}
